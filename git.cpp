#include <iostream>
#include <string>

using namespace std;
int main(void)
{

    string text[] = {"green", "vs", "yellow", "bubble"};
    int length = size(text);
    for (int i = 0; i < length; i++)
    {
    }

    return 0;
}
